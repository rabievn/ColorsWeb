const cols = document.querySelectorAll('.col')

function generateRandomColor() {
    // RGB
}

function setRandomColors() {
    cols.forEach(col => {
        console.log(col);
    })
}
setRandomColors()